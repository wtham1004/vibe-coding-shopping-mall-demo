# --- API ---
# 프로덕션: client/.env.production 의 VITE_API_URL (vite build 시 로드, 저장소에 커밋됨).
# 로컬만 다른 백엔드 쓰려면 client/.env 또는 .env.local 에 VITE_API_URL 을 두면 개발 서버가 우선합니다.
# Heroku API 베이스 URL (끝 슬래시 없음, https).
# 로컬 개발: 비우면 Vite 프록시(vite.config.ts 의 /api → 백엔드 포트) 사용.

# --- Cloudinary (이미지 업로드) — 대시보드에서 클라이언트 업로드 허용 프리셋 사용 ---
VITE_CLOUDINARY_CLOUD_NAME=your_cloud_name
VITE_CLOUDINARY_UPLOAD_PRESET=your_unsigned_preset

# --- PortOne(아임포트) — 프로덕션 도메인을 콘솔에 등록 ---
# VITE_IAMPORT_IMP_CODE=impxxxxxxxx
